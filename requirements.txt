# Root requirements.txt — local development only.
# NOT used by sam build (SAM reads src/orchestrator/requirements.txt).
#
# Training dependencies (peft, transformers, trl, bitsandbytes, etc.)
# are intentionally NOT listed — they are installed on EC2 at runtime
# by src/training/bootstrap.sh.

boto3>=1.34.0
