[pytest]
testpaths = tests
# Lambda code is packaged flat from src/orchestrator/ and the training helpers
# are uploaded flat to the code bucket, so both import as top-level modules.
pythonpath = src/orchestrator src/training
